packages:
  - "packages/*"

allowBuilds:
  esbuild: true
  unrs-resolver: false

# First-party packages — no supply-chain cooldown for our own releases
minimumReleaseAgeExclude:
  - "@sebastian-gmbh/*"
  - "@sebastian-software/*"
  - ardo
